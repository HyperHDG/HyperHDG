# ne1-graphs

## data collection

compute graph diameter, girth, ...

backend = (KaFFPa NAIVE)
ns = $(seq 2 2 40)

maybe not for all...
  delta = $(seq 1 10)

## graphs

figure out the constants from the paper (sigma, rho, R)

TODO:compare min, first quartile, median, third quartile, max instead of min, avg, max, stddev
SimpleStats stat_* can be displayed as [---[[]]---]

first everything for small network, then for large

1.1
graph of partitioner_time over number of partitions for both backends (2 lines)
  -> partitions go from 4 to 900?
  -> expect naive to be constant (only depends on number of vertices),
     while kahip should be like p * log p for p=#partitions

1.2
graph of (size-) stats_before, stats_after and both backends over number of partitions (4 lines)
  -> maybe split it into sub figures/tables
  -> partitions go from 4 to 900?

1.3
graph of (size-) stats_frac for both backends over number of partitions (2 lines)
  -> partitions go from 4 to 900?
  -> expect that naive has much higher fracs, as it is poorly connected internally, but quickly becomes well connected, while algebraic should take longer
  -> maybe plot simultaneously for multiple deltas

1.4
graph of (size-) stats_frac for both backends over delta parameter for fixed number of partitions?
  together with
number of iterations
  -> expect

1.5
graph as scatter plot the (stats) iteration time over sizes after for both backends
  -> double boxplot, backends in different colors

1.6
graph number of iterations over number of partitions with both backends
together with iteration time (look into accurately measuring this)
  -> tie into graph for sizes
